/* eslint-disable no-useless-assignment */
import path from 'path';

export const DB_FILE = path.join(process.cwd(), 'ham_aistudio_projects.json');
export const HAMLI_MEMORY_FILE = path.join(process.cwd(), 'hamli_memory.json');
export const APP_STATUS_FILE = path.join(process.cwd(), 'app_status.json');
export const TEMP_PROJECTS_DIR = path.join(process.cwd(), 'temp_web_projects');
