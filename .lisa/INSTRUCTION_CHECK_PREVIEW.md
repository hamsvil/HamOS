# INSTRUCTION FOR LISA: Check Preview Tool & Setup Periodic Screenshot

Hello Lisa,

Please perform the following verification and setup tasks:

1. Validate if the `verifyPreview` (preview & screenshot) tool is working correctly. You can test it by running `npm run verify:preview` or executing the underlying script.
2. If it is working correctly, please set up a periodic/recurring task to update the latest screenshot of the **main application menu**.
3. Save these periodic screenshots within the `.lisa/preview/` folder (e.g. `screenshot.png`).
4. Report your results and periodic setup status to the `.lisa/app_status.json` or SharedMemory.

Thanks,
Main Agent
